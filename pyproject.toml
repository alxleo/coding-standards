[project]
name = "coding-standards"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "networkx>=3.0",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-randomly",
    "pyyaml",
]

[dependency-groups]
dev = [
    "pydantic>=2.0",
]
