{
  "test_commands": [
    "trivy --version",
    "pmd --version",
    "caddy version",
    "just --version",
    "conftest --version",
    "npx commitlint --version || true",
    "pyright --version",
    "zizmor --version",
    "knip --version",
    "dclint --version",
    "tsc --version",
    "depcruise --version",
    "license-checker --version || true",
    "vulture --version",
    "deptry --version",
    "lint-imports --help",
    "oxlint --version",
    "type-coverage --version",
    "publint --help",
    "attw --version",
    "rumdl version",
    "shfmt --version",
    "ruff --version",
    "codespell --version",
    "ansible-lint --version",
    "sqlfluff version",
    "eslint --version",
    "prettier --version",
    "v8r --help",
    "shellcheck --version",
    "hadolint --version",
    "actionlint --version",
    "gitleaks version",
    "tflint --version",
    "editorconfig-checker --version || editorconfig-checker -version || true",
    "kubeconform -v",
    "lychee --version",
    "golangci-lint --version",
    "stylelint --version",
    "htmlhint --version",
    "ls-lint --version || true",
    "spectral --version",
    "checkmake --version",
    "semgrep --version",
    "conftest verify -p /opt/coding-standards/policies/repo-standards/",
    "conftest verify -p /opt/coding-standards/policies/compose/",
    "semgrep scan --config /opt/coding-standards/semgrep-rules/ --validate",
    "cd /tmp && python3 /opt/coding-standards/scripts/generate_repo_manifest.py && test -f repo-manifest.json"
  ]
}
