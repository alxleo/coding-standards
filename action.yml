# coding-standards — setup action (used internally by the reusable workflow).
#
# Copies baked-in linter configs into the workspace.
# Consumer repos should NOT call this directly — use the reusable workflow instead.
#
# See .github/workflows/lint.yml for the main entry point.
name: coding-standards-setup
description: Copy centralized linter configs into the workspace

inputs:
  config-file:
    description: 'Path to override config file in consumer repo'
    required: false
    default: '.coding-standards.yml'
  skip-hooks:
    description: 'Comma-separated linter groups to skip'
    required: false
    default: ''

outputs:
  skip-hooks:
    description: 'Merged skip list (input + override file)'
    value: ${{ steps.setup.outputs.skip-hooks }}

runs:
  using: composite
  steps:
    - name: Parse overrides
      id: setup
      shell: bash
      env:
        CONFIG_FILE: ${{ inputs.config-file }}
        INPUT_SKIP: ${{ inputs.skip-hooks }}
      run: |
        set -euo pipefail
        SKIP_FROM_OVERRIDE=""
        if [ -f "$CONFIG_FILE" ]; then
          echo "Found override file: $CONFIG_FILE"
          if command -v python3 &>/dev/null; then
            SKIP_FROM_OVERRIDE=$(python3 -c "
        import yaml, sys
        try:
            with open('$CONFIG_FILE') as f:
                cfg = yaml.safe_load(f) or {}
            hooks = cfg.get('skip-hooks', [])
            print(','.join(hooks) if isinstance(hooks, list) else str(hooks))
        except Exception:
            print('')
        " 2>/dev/null || echo "")
          fi
        fi
        SKIP=""
        if [ -n "$INPUT_SKIP" ]; then
          SKIP="$INPUT_SKIP"
        fi
        if [ -n "$SKIP_FROM_OVERRIDE" ]; then
          if [ -n "$SKIP" ]; then
            SKIP="$SKIP,$SKIP_FROM_OVERRIDE"
          else
            SKIP="$SKIP_FROM_OVERRIDE"
          fi
        fi
        echo "skip-hooks=$SKIP" >> "$GITHUB_OUTPUT"
        if [ -n "$SKIP" ]; then
          echo "Will skip: $SKIP"
        fi

    - name: Apply coding-standards configs
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail

        # Only copy configs that don't already exist (consumer can override)
        configs=(
          .gitleaks.toml
          .markdownlint-cli2.yaml
          .shellcheckrc
          .yamllint
          .hadolint.yaml
          .jscpd.json
          .prettierrc
          .editorconfig
          .mega-linter.yml
          commitlint.config.mjs
        )
        for cfg in "${configs[@]}"; do
          if [ ! -f "$cfg" ]; then
            cp "$ACTION_PATH/lint-configs-626465/$cfg" "$cfg"
            echo "  Applied: $cfg"
          else
            echo "  Kept (consumer override): $cfg"
          fi
        done

        # Always apply pre-commit config — this IS the standard
        cp "$ACTION_PATH/lint-configs-626465/.pre-commit-config.yaml" .pre-commit-config.yaml
        echo "  Applied: .pre-commit-config.yaml"

        # Apply custom hook scripts
        mkdir -p scripts/hooks
        cp "$ACTION_PATH/scripts/hooks/"* scripts/hooks/ 2>/dev/null || true
        chmod +x scripts/hooks/* 2>/dev/null || true
        echo "  Applied: scripts/hooks/*"
